# Sections 1–8 launch-readiness evidence matrix

Verified: 2026-07-28 (America/New_York)

This matrix distinguishes repository proof from external evidence. `PASS` means the cited automatic
check ran successfully against the current working tree. `BLOCKED` never means pass. The canonical
machine-readable run is `build/release-evidence/app-store-sections-1-8.json`; after the final commit
it records the exact Git commit and whether the tree is dirty. The logical commit hashes are added
to the build evidence during handoff; local verification is rerun against the final clean `HEAD`.

## Automated and repository-verifiable gates

| Gate                               | Status         | Executed evidence                                                                                                                                                                                                                                                    |
| ---------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Migration numbering/reconciliation | PASS           | `npm run check:migrations`: 53 unique active versions `0001`–`0053`; both historical `0021` hashes preserved; canonical and forward reconciliation present.                                                                                                          |
| Generated compliance artifacts     | PASS           | `npm run compliance:check`: 30 generated artifacts match structured sources.                                                                                                                                                                                         |
| Required code/config/docs          | PASS           | Release verifier confirms 34 required artifacts, including this matrix, production/auth verifiers, adult policy, legal routes, inventories, native manifest, deletion and retention workers.                                                                         |
| Legal-version consistency          | PASS           | Configured versions match migration seeds and runtime constants.                                                                                                                                                                                                     |
| Adult eligibility                  | PASS           | Unchecked UI attestation, exact server metadata, Auth-trigger enforcement for every provider, existing-user gate, and under-18 path are present; unit/browser checks pass.                                                                                           |
| Authentication hardening           | PASS           | Request and server AAL2 gates, TOTP challenge, recovery route, 12-character new-password policy, rate limits, global session revocation, and a dedicated non-cron admin credential are present.                                                                      |
| Durable deletion design            | PASS           | Provider grant revocation precedes local token/data deletion; Auth is last; durable claims, retry/backoff, reauthentication, idempotent Auth retry, and atomic receipt/job finalization are implemented and tested.                                                  |
| RLS declarations                   | PASS           | Static release check finds RLS enabled for all 75 public tables created by active migrations. This is not a substitute for the blocked pgTAP runtime check.                                                                                                          |
| Logging and telemetry boundary     | PASS           | Direct application console calls are absent; classified logger, sensitive-key rejection, Sentry scrub, analytics allowlist, and neutral-notification tests pass.                                                                                                     |
| Client secret boundary             | PASS           | Release check finds no server credential names in client modules. Secretlint quick-start scan passes with masked output.                                                                                                                                             |
| Production feature surface         | PASS           | Nest, social/household, and subscription surfaces are source-locked off; all disabled Nest mutations reject server-side.                                                                                                                                             |
| AI egress boundary                 | PASS           | All OpenAI calls consume server-issued purpose/category permits; fixed destination, size, timeout, and provider registry checks are enforced; no alternate client path is found.                                                                                     |
| Analytics allowlist                | PASS           | Only registered events and primitive non-sensitive metadata are accepted.                                                                                                                                                                                            |
| Native release endpoint            | PASS           | Capacitor uses the fixed production HTTPS origin and disables cleartext.                                                                                                                                                                                             |
| HealthKit purpose strings          | PASS           | Source strings state categories, server transfer, read-only access, and independent health-AI consent.                                                                                                                                                               |
| Source privacy manifest            | PASS           | Inventoried data classes, no tracking, no precise location, and no app-direct required-reason APIs are declared.                                                                                                                                                     |
| Notification privacy               | PASS           | Push and email bodies are neutral and omit health, calendar, prompt, and check-in details.                                                                                                                                                                           |
| Minor data minimization            | PASS           | Runtime birth-year collection is removed; migration `0053` restricts known minors, revokes consent, queues deletion, and erases birth year.                                                                                                                          |
| Codemagic release automation       | PASS           | The pinned workflow runs clean install, source/build checks, fail-closed production verification, native preparation, archive/signing/privacy validation, artifact retention, and TestFlight upload only with protected credentials.                                 |
| Native automated-test preparation  | PASS           | Stable accessibility identifiers and an explicitly nonproduction XCUITest target cover launch, adult gate, optional staging auth, screenshots, and accessibility audit without a release reset/backdoor.                                                             |
| Physical-device test package       | PASS           | A structured 70-scenario matrix, owner form generator, and fail-closed validator require an exact commit/build/IPA hash/device record and reject blank, failed, or incomplete evidence.                                                                              |
| Unit/static/build suite            | PASS           | `typecheck`, `lint`, 39 test files / 252 tests, full and production dependency audits, clean lockfile install, secret scans, and Next.js 16.2.12 production build pass.                                                                                              |
| Rendered public surface            | PASS           | Built-server checks cover home, login, unchecked signup attestations, under-18 path, Privacy, Terms, legal index and six legal policies, Security, and `security.txt`; content, navigation, titles, overlays, browser errors, console, and screenshots were checked. |
| Sign in with Apple                 | NOT APPLICABLE | V1 authentication is email/password. Google is a post-login Calendar connector, not account authentication; see `docs/architecture/sign-in-with-apple-decision.md`. Reassess before adding social login.                                                             |

## External/manual closure gates

| Gate                         | Status  | Exact closer and evidence                                                                                                                                                                                                                                                        | Risk if skipped                                                                                 |
| ---------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| Production legal identity    | BLOCKED | Business owner and counsel must supply all 15 non-placeholder production values, matching public documents and App Store Connect; a release verifier run with those protected values must pass.                                                                                  | Wrong seller/operator/contact/jurisdiction or a production build failure.                       |
| Processor approvals          | BLOCKED | Privacy/security owner must approve each production processor in `config/privacy/processors.json`, attach dated contract/DPA/security/region/retention evidence, and enable only approved production entries.                                                                    | Undisclosed or uncontracted health/personal-data processing.                                    |
| AI provider approvals        | BLOCKED | Privacy/security owner and counsel must approve OpenAI and LogMeal data controls, retention, training, regions, contracts/DPAs, owners, and expiration dates in the registry.                                                                                                    | Sensitive data sent under unapproved terms; production correctly remains fail-closed meanwhile. |
| Attorney approval            | BLOCKED | Qualified counsel must review the exact public documents, entity, jurisdictions, adult-only model, consumer-health laws, breach duties, Terms, and privacy claims; add a dated non-secret approval record at the configured evidence path.                                       | Unenforceable or inaccurate legal terms and regulatory exposure.                                |
| Manual license obligations   | BLOCKED | Counsel/license owner must resolve the enumerated LGPL, compound, FSL, MPL, and CC-BY obligations and add evidence referenced by `config/legal/license-policy.json`.                                                                                                             | Distribution without satisfying source, notice, attribution, or commercial-use conditions.      |
| Asset ownership              | BLOCKED | IP owner must provide assignment/license/provenance records for the bird collection and app icon/launch artwork at the configured evidence paths.                                                                                                                                | Copyright/trademark dispute or takedown.                                                        |
| Production migration history | BLOCKED | Authorized database operator must take/verify a backup, run `scripts/discover-migration-state.mjs` read-only against production, review the legacy `0021` fingerprint, and save sanitized JSON at `docs/launch-readiness/evidence/database/production-migration-discovery.json`. | Applying the wrong reconciliation to an unknown production history.                             |
| Clean database integration   | BLOCKED | Docker Desktop or an isolated Supabase/Postgres runner must execute `npm run test:db` for fresh and representative legacy-upgrade fixtures plus pgTAP/RLS; store the generated success JSON at the expected database evidence path.                                              | SQL syntax, ordering, RLS, or upgrade defects could remain undiscovered.                        |
| Staging deletion proof       | BLOCKED | Authorized staging operator must create a representative account with OAuth grants, storage, notifications, and relational data; delete it, confirm provider revocation and no residue, test retry, and store sanitized evidence at the expected path.                           | Account or provider data may survive a real deletion request.                                   |
| Auth provider configuration  | BLOCKED | Supabase project administrator must verify confirmation, recovery, breached-password/password policy, rate limits, redirect allowlist, session rotation, global logout, and staged TOTP AAL behavior; store sanitized configuration/test evidence.                               | Dashboard settings could undermine code-level auth controls.                                    |
| Signed iOS archive           | BLOCKED | Codemagic/macOS with Apple credentials must build the exact tree, validate team/profile/HealthKit/signature/version/dSYM, and retain the sanitized archive JSON plus artifact hashes.                                                                                            | Unsigned, mis-entitled, unreproducible, or crash-symbol-free candidate.                         |
| Xcode privacy report         | BLOCKED | Apple build owner must export the aggregate report from the exact signed candidate, reconcile every SDK/data/required-reason declaration, and attach sanitized review evidence.                                                                                                  | App Store rejection or inaccurate privacy declarations.                                         |
| Physical iPhone matrix       | BLOCKED | QA with a supported physical iPhone must execute the documented HealthKit no/partial/full/empty/revoked, camera, photo, offline, OAuth, notification, consent, export, and deletion matrix and attach signed results.                                                            | Device-only permission, lifecycle, or data-loss failures can reach users.                       |

## Environment-limited checks

- `npm run test:db:preflight` exits blocked because Windows `WSLService` is disabled/stopped and
  Docker Engine is unreachable; no database success evidence was written and no remote command ran.
- `npm run ios:validate` cannot start Windows Subsystem for Linux in this environment. Repository
  source checks pass, but the actual validator remains a macOS/Codemagic candidate gate.
- CodeQL, dependency review, and Gitleaks are blocking GitHub workflows. Local pinned Gitleaks and
  Secretlint scans passed; CI completion for the final pushed commit is still required.
